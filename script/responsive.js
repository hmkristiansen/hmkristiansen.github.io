$(window).resize(function() { 
    let w = window.innerWidth;
    let h = window.innerHeight;
    let ratio = w/h;
    //console.log(ratio);

    if(ratio >= 1){
        //desktop view
    }else{
        //mobile view
    }

});